{
    "name": "telegram-video-merger-bot",
    "description": "Ultimate File Management Bot for Telegram",
    "type": "project",
    "require": {
        "php": "^8.1",
        "ext-curl": "*",
        "ext-json": "*",
        "ext-gd": "*",
        "ext-zip": "*",
        "ext-mbstring": "*"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "post-install-cmd": [
            "chmod 666 users.json metadata.json bot_state.json error.log || true",
            "mkdir -p uploads temp && chmod 777 uploads temp"
        ]
    }
}
